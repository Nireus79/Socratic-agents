[build-system]
requires = ["setuptools>=65.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "socratic-agents"
version = "0.1.0"
description = "Multi-agent orchestration system with 19 specialized agents for AI workflows. Built-in integrations with Openclaw, LangChain, and Socratic Nexus."
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Socratic Agents Contributors"}
]
keywords = [
    "agents",
    "ai",
    "orchestration",
    "multi-agent",
    "langchain",
    "openclaw",
    "production",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
requires-python = ">=3.9"

dependencies = [
    "pydantic>=2.0.0",
    "loguru>=0.7.0",
    "socratic-maturity>=0.1.0",
]

[project.optional-dependencies]

# Framework integrations
openclaw = []
langchain = ["langchain>=0.1.0"]
nexus = ["socrates-nexus>=0.3.6"]

# Socratic ecosystem integrations
conflict = ["socratic-conflict>=0.1.0"]
learning = ["socratic-learning>=0.1.0"]

# All features (maturity is now a core dependency)
all = [
    "langchain>=0.1.0",
    "socrates-nexus>=0.3.6",
    "socratic-conflict>=0.1.0",
    "socratic-learning>=0.1.0",
]

# Development
dev = [
    "pytest>=7.0",
    "pytest-asyncio>=0.21.0",
    "pytest-cov>=4.0",
    "pytest-benchmark>=4.0",
    "black>=23.0",
    "ruff>=0.1.0",
    "mypy>=1.0",
]

[project.urls]
Homepage = "https://github.com/Nireus79/Socratic-agents"
Repository = "https://github.com/Nireus79/Socratic-agents"
Documentation = "https://github.com/Nireus79/Socratic-agents#readme"
Issues = "https://github.com/Nireus79/Socratic-agents/issues"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages]
find = {where = ["src"]}

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = "test_*.py"
python_classes = "Test*"
python_functions = "test_*"
markers = [
    "unit: Unit tests",
    "integration: Integration tests",
    "slow: Slow tests",
    "benchmark: Performance benchmark tests",
    "memory: Memory usage tests",
]
addopts = "-v --strict-markers"

[tool.black]
line-length = 100
target-version = ["py38"]

[tool.ruff]
line-length = 100
target-version = "py38"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.9"
warn_return_any = false
warn_unused_configs = true
disallow_untyped_defs = false
disallow_incomplete_defs = false
ignore_missing_imports = true
